/*
Cin return the string to cin but during the cin can help the user to do his choice

*/
#include <string>
#include <vector>
#include <map>
#include <Windows.h>

std::string cin(std::vector<std::string>, std::map<std::string, std::vector<std::string>>);
