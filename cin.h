/*
Cin return the string to cin but during the cin can help the user to do his choice

*/
#include <string>
#include <vector>
#include <map>
#include <Windows.h>

void bcin(std::string&, std::string, std::vector<std::vector<std::string>>);
